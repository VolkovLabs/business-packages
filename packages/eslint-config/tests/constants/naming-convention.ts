/* eslint-disable @typescript-eslint/no-unused-vars */

/**
 * Global Constant
 */
const GLOBAL_CONSTANT = {};

/**
 * Property
 */
const ADMIN_USER = {
  firstName: 'John',
};

/**
 * Enum
 */
enum Status {
  ACTIVE = 'active',
  PAUSED = 'paused',
}
